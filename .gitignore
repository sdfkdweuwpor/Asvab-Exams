node_modules/
.DS_Store
*.log

# The source PDF stays tracked at source/asvab-source.pdf while the migration
# runs, so extraction is reproducible. It is removed at M6, once the bank is
# final -- the app never loads it.
